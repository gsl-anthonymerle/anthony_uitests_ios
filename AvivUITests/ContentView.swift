import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("UITest is not activated")
                .font(.title)
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
